const request = require('supertest');
const app = require('../4-http');


test('Tests the requests for the simple HTTP server', () => {
  // const expectedOutput = 'Hello Holberton School!';

  // request(app)
  // .get('/does_not_exist')
  // .expect('Content-Type', 'text/plain')
  // .expect(200)
  // .expect(expectedOutput)
  // .then((res) => {
  //   expect(res.body).toEqual(expectedOutput);
  //   done();
  // })
});
